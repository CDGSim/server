<!doctype html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover"> 
        <title>#import("title")</title>
        <link rel="stylesheet" media="all" href="/stylesheets/main.css" />
    </head>

    <body>
        <nav id="globalnav"">
            <h1><a href="/">Simulations</a></h1>
            #if(simulation_properties.update_date):<h3>Mis à jour le #date(simulation_properties.update_date, "dd/MM/YYYY")</h3>#endif
        </nav>
        
        <main>
                    
            #import("main")

        </main>

    </body>
</html>
